import React from 'react';
import {
    SafeAreaView,
    ScrollView,
    StatusBar,
    StyleSheet,
    Text,
    useColorScheme,
    View,
} from 'react-native';

export default function SignUp(props) {

    const { text } = props;

    return (

        <View>
            <Text>Sign Up</Text>
        </View>
    );

}